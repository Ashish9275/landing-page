function Header() {
  return (
    <header className="header">
      <h2>MyLanding</h2>
      <nav>
        <a href="#features">Features</a>
        <a href="#contact">Contact</a>
      </nav>
    </header>
  );
}

export default Header;
